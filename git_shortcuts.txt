git shortcuts

the `git remote add` command takes two arguments:

A remote name, for example, origin
A remote URL, for example, https://github.com/user/repo.git

===TO ADD AN UPSTREAM===
git remote add upstream https://github.com/DataBrewery/cubes.git
git fetch upstream

===TO DIFF BETWEEN TWO REMOTES===
git diff origin/master upstream/master

===TO MERGE A FORK TO THE NEW HEAD===
git fetch upstream
git checkout master # should already be on master
git merge upstream/master

===TO SET UP A REMOTE TRACKING BRANCH(NOT MASTER)===
git checkout -tb local_branch_name remotes/origin/tracked_branch_name

===TO GET THE URL OF A REPO===
git remote show origin
git remote show upstream

=== enterprise ===
git checkout --track origin/serverfix
git remote prune origin
git branch -m newbranch

=== HEROKU ===
1) heroku create  add_a_name (prepare heroku to revcieve code)
2) git push heroku master (deploy code)
3) heroku ps:scale web=1 (start it)
4) heroku logs (view logs)


=== More general HEROKU ===
# Collectstatic
heroku run python manage.py collectstatic --noinput

# Set sync and migrate the database
heroku run python manage.py syncdb
heroku run python manage.py migrate

# Set environment variable on Heroku
heroku config:set DEBUG=True


=== to run my local heroku
heroku config:set SECRET_KEY=?????
heroku config:set ADMINS=jdumblauskas@gmail.com
heroku config:set ENVELOPE_EMAIL_RECIPIENTS=jdumblauskas@gmail.com
heroku config:set DEBUG=False
heroku config:set ALLOWED_HOSTS="jerry-chipy.herokuapp.com"


